# Optional site URL for canonical tags, sitemap, and metadata
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# AdSense review mode: configure the client ID but keep enabled=false.
# This emits the verification meta tag without loading ad-serving scripts.
NEXT_PUBLIC_ADSENSE_ENABLED=false
NEXT_PUBLIC_ADSENSE_CLIENT_ID=ca-pub-0000000000000000

# Manual responsive units. Set only after AdSense marks the site Ready.
NEXT_PUBLIC_ADSENSE_SIDEBAR_SLOT_ID=
NEXT_PUBLIC_ADSENSE_TOOL_SLOT_ID=
NEXT_PUBLIC_ADSENSE_ARTICLE_SLOT_ID=

# Server-only TomTom Search API key for global address autocomplete.
# Add the real value to .env.local and Vercel; never prefix it with NEXT_PUBLIC_.
TOMTOM_SEARCH_API_KEY=
